{{- if .Values.abcWebShell.enabled  }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.abcWebShell.serviceAccount.name }}
  namespace: {{ .Release.Namespace | default "quantanexus" }}
  labels:
    app: abc-webshell
    {{- include "quantanexus.labels" . | nindent 4 }}
  
{{- end }}