service:
  ui:
    type: NodePort          # 集群外访问,QN默认从32600开始占用端口，方便用来写文档测试。
    nodePort: 32600         # 物理节点监听端口（可改）
    port: 80                # ClusterIP 端口
  manager:
    type: ClusterIP
  backend:
    type: ClusterIP

# 让 Longhorn 成为默认 StorageClass
persistence:
  defaultClass: true
  defaultClassReplicaCount: 2   # 根据节点数调整
  defaultFsType: ext4
  reclaimPolicy: Delete

# 其余常用调优
defaultSettings:
  defaultDataPath: /var/lib/longhorn
  createDefaultDiskLabeledNodes: true
  defaultDiskPath: /var/lib/longhorn
  createDefaultDisk: true
  defaultDiskAllowScheduling: true
  
# custom-images.yaml
image:
  # 核心组件
  longhorn:
    engine:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-longhorn-engine
    manager:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-longhorn-manager
    ui:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-longhorn-ui
    instanceManager:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-longhorn-instance-manager
    shareManager:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-longhorn-share-manager
    backingImageManager:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-backing-image-manager
    supportBundleKit:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-support-bundle-kit
  # CSI 组件
  csi:
    attacher:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-csi-attacher
      # tag: v4.9.0-20250826 # 保持原样，无需修改
    provisioner:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-csi-provisioner
      # tag: v5.3.0-20250826 # 保持原样，无需修改
    nodeDriverRegistrar:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-csi-node-driver-registrar
      # tag: v2.14.0-20250826 # 保持原样，无需修改
    resizer:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-csi-resizer
      # tag: v1.14.0-20250826 # 保持原样，无需修改
    snapshotter:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-csi-snapshotter
      # tag: v8.3.0-20250826 # 保持原样，无需修改
    livenessProbe:
      registry: harbor.hi168.com/quantanexus
      repository: longhornio-livenessprobe
      # tag: v2.16.0-20250826 # 保持原样，无需修改
