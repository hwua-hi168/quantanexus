mode: distributed #分布式
auth:
  rootUser: admin
  rootPassword: "admin"  
replicas: 4 #4个pod
statefulset:
  replicaCount: 4
resources:
  requests:
    memory: 20Gi
  limits:
    memory: 40Gi
persistence:
  size: 100Gi
service:
  type: NodePort
consoleService:
  type: NodePort
