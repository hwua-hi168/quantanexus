apiVersion: v1
kind: Service
metadata:
  name: pushgateway
  namespace: prom
  labels:
    app: pushgateway
spec:
  selector:
    app: pushgateway
  type: NodePort
  ports:
    - name: pushgateway
      port: 9091
      targetPort: 9091
      nodePort: 30091
      protocol: TCP